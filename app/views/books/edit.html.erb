<h2 class="edit-font">Editing Book</h2>
<%= form_for(@book) do |f| %>
	<h4 class="form-font">Title</h4>
	<%= f.text_field :title %>
	<h4 class="form-font">Body</h4>
	<%= f.text_area :body %>

	<%= f.submit "Update Book" %>

<% end %>
<%= link_to "Show", book_path(@book.id) %>
｜
<%= link_to "Back", books_path %>
